# Backend (Maven / Spring Boot)
backend/target/
backend/.mvn/
backend/*.log
backend/HELP.md

# Frontend (Node / Vite)
frontend/node_modules/
frontend/dist/
frontend/.vite/
frontend/*.local

# Env files (never commit real secrets)
.env
*.env
!.env.example
backend/.env
frontend/.env

# IDE / OS
.idea/
.vscode/
*.iml
.DS_Store

# Claude Code local settings (per-machine permission grants, not app config)
.claude/settings.local.json

# Logs
*.log
npm-debug.log*
