package com.clothingretail.auth;

/** The fixed set of roles supported for now. No fine-grained permission model yet. */
public enum RoleName {
    SUPER_ADMIN,
    ADMIN,
    EMPLOYEE,
    CUSTOMER
}
