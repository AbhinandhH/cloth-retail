package com.clothingretail.returns;

/** What a {@link ReturnRequest} is asking for - drives which fields/flow apply, see ReturnRequest's own doc comment. */
public enum RequestType {
    SIZE_EXCHANGE,
    DAMAGED_PRODUCT
}
